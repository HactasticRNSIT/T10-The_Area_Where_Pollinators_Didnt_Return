# ── Dev / security audit dependencies ────────────────────────────────────────
# Install: pip install -r requirements-dev.txt
# Run audit: pip-audit -r requirements.txt
pip-audit>=2.7.0,<3.0.0
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
httpx>=0.27.0,<1.0.0
